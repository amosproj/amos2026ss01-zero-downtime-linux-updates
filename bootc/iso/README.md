We use `bootc-image-builder` https://osbuild.org/docs/bootc/

we do not use the newer
osbuild.org `image-builder`: - "container-native ISO contract", bootloader/kernel/GRUB defined _inside_ container instead of letting anaconda installer do that during install - https://github.com/ondrejbudai/bootc-isos - seems to not be mature/stable, only noted here for future evaluation

..
