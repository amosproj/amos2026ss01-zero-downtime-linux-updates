{
    "id": "custom-setup",
    "version": "1.0.0",
    "name": "Custom Setup",
    "description": "Provides additional container setup for the AMOS Project 1 Dev Container",

    // Rust installation is costly, reuse cache when possible
    "installsAfter": [
        "ghcr.io/devcontainers/features/rust"
    ],

    "options": {
        "lastChanged": {
            "type": "string",
            "description": "Marker option of when the install script was last changed",
            "proposals": ["2026-04-24 22:40"],
            "default": ""
        }
    }
}