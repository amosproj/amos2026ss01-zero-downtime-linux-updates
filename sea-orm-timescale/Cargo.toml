[package]
name = "sea-orm-timescale"
version = "0.2.0"
edition = "2024"
description = "TimescaleDB extension for Sea-ORM: time_bucket, hypertables, compression, continuous aggregates"
license = "MIT"
repository = "https://github.com/evanjt/sea-orm-timescale"
keywords = ["sea-orm", "timescaledb", "time-series", "postgresql"]
categories = ["database"]

[dependencies]
sea-orm = { version = "2.0.0-rc", default-features = false }
sea-query = "~1.0.0"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
sea-orm = { version = "2.0.0-rc", features = ["sqlx-postgres", "runtime-tokio-rustls", "macros", "with-chrono", "with-uuid"] }
tokio = { version = "1", features = ["full"] }
uuid = { version = "1", features = ["v4"] }
