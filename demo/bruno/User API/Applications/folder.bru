meta {
  name: Applications
  seq: 4
}

docs {
  The application model has three layers:

  - **Application** (here) — catalog entry, name + description only.
  - **App Config** — a versioned deployable: container image +
    configuration for a device or group.
  - **App Assignment** — declares a config as desired state for a device
    or group.
}
