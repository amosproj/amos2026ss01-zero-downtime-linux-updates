meta {
  name: Get Device
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/devices/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

docs {
  `GET /devices/{id}` — a single device by ID. 404 if unknown.
}
