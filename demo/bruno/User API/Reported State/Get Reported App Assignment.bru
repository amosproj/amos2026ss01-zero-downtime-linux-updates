meta {
  name: Get Reported App Assignment
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/reported-app-assignments/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

docs {
  `GET /reported-app-assignments/{id}` — a single report by ID.
}
