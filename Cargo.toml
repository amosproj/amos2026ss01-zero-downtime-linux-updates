[workspace]
resolver = "3"
members = ["api-mock-server", "common"]

[workspace.dependencies]
amos-api-mock-server = { path = "api-mock-server" }
amos-common = { path = "common" }

serde_json = "1.0"
serde = { version = "*", features = ["derive"] }
