meta {
  name: Create Group
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "production-line-1"
  }
}

docs {
  `POST /groups` — create a group. Returns 201.
}
