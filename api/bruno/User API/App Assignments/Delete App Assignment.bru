meta {
  name: Delete App Assignment
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/app-assignments/:id
  body: none
  auth: inherit
}

params:path {
  id: 999
}

docs {
  `DELETE /app-assignments/{id}` — undeclare the app for its target.
  Returns 204.
}
