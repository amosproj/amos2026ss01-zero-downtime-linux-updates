meta {
  name: Audit Logs
  seq: 12
}

docs {
  Database triggers record every change to tracked tables — operation,
  changed fields, the authenticated user (from the JWT) and a timestamp.
  Read-only.
}
