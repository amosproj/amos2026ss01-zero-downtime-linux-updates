meta {
  name: OS Versions
  seq: 7
}

docs {
  Catalog of bootable system images. `commit_hash` is the bootc image
  reference (e.g. a `ghcr.io/…-system:<tag>` OCI ref or an ostree
  checksum); `orchestrator_version` records which orchestrator ships in
  that image.
}
