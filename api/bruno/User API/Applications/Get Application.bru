meta {
  name: Get Application
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/applications/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

docs {
  `GET /applications/{id}` — a single application by ID.
}
