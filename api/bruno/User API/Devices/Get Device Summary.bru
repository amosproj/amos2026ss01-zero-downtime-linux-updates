meta {
  name: Get Device Summary
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/devices/:id/summary
  body: none
  auth: inherit
}

params:path {
  id: 1
}

docs {
  `GET /devices/{id}/summary` — identity, tenant and reported state
  (running OS, running apps) for a single device.
}
