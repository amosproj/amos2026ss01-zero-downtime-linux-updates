The Device API is small and modelled in this singular OpenAPI file.
The file gets used in the GitHub Action "Docs", where it gets deployed as a Swagger UI page.
An editor like [the one by Swagger](https://editor.swagger.io/) makes it much easier to modify this file.